# Vite only loads .env files from this (frontend/) directory, and only
# VITE_-prefixed variables are exposed to client code — see vite.config.ts
# and src/api/client.ts.

# Base URL of the backend API, no trailing slash.
VITE_API_URL=http://localhost:8000
